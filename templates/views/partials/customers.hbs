{{!< default}}
<div class="container">
	<h1>Customers</h1>
	<hr>
	<div class="row">
		<div class="col-md-12 cold-lg-12">
			<div class="row">
				{{# each customers}}
					<div class="col-md-4 col-lg-4">
						{{> customer this}}
					</div>
					{{/each}}
			</div>
		</div>
	</div>
</div>
